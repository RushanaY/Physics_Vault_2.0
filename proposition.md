an expression or a statement, that we can assign a truth value to
to be noted: it is a certain value, something that can be evaluated. 
properties:
 - well formedness 
- extensibility -/ truth value depends only on its components propositioins (you can determine if it;s tue oif false only from looking at the given statement)