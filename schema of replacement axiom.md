$$N := \{y | \exists x \in M: \phi(x, y)\}$$ we have $\phi(x,y) : = (x =y) \land \phi(x)$ as a special case 

for all elements of set M, there is at least one y, for that  $\phi(x,y)$ is true 
it does remind us a lot of functions, like when we map an element x onto an element y form the other set 